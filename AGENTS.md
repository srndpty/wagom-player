# Wagom Player 開発ガイド

## コミュニケーション

- AIチャットの返答、コードコメント、docstring、ログ、例外・UI文言、開発文書、コミットのタイトルと本文など、人間が読む文章は日本語で統一する。
- 識別子、外部API名、プロトコル名、Conventional Commitsのtype prefixは英語のままでよい。
- 判断の背景や制約は、将来の保守に必要なものだけを簡潔に残す。

## 設計

- DRY、単一責任、関心の分離を優先し、同じ判断や状態を複数箇所で管理しない。
- 依存方向は `domain <- application <- ui / infrastructure` とする。domainはQt、VLC、OS、ファイルシステムへ依存させない。
- domainは純粋な規則と状態、applicationはユースケース、infrastructureは外部システムとの接続、uiは表示とユーザー入力を担当する。
- UIクラスへ業務判断、ファイル操作、永続化処理を直接追加しない。明示的なサービスやコントローラーへ委譲する。
- 公開境界では型ヒントと意味のある結果型を使い、文字列や真偽値だけで複数の状態を表さない。
- 依存はコンストラクターなどから注入し、単体テストで実装を差し替えられるようにする。
- 過度な抽象化や将来を推測した拡張点は作らず、現在必要な最小の設計にする。

## 互換性とエラー処理

- 開発中のため、旧import、非推奨API、互換ラッパー、推測的なフォールバックは原則として残さない。
- 必須依存の不足を明確に通知する処理や、サポート対象外環境で安全に停止する処理は維持する。
- 例外を握りつぶさない。回復可能な例外だけを具体的に捕捉し、利用者向け通知と診断情報を分離する。
- 外部I/OやVLC操作では失敗、タイムアウト、再入、古い非同期callbackを明示的に扱う。

## 実装とテスト

- 変更は小さく焦点を絞り、無関係な整形や仕様変更を混ぜない。
- 振る舞いを変更・移動した場合は、正常系、境界値、失敗系、再入・非同期競合を必要に応じてテストする。
- QtやVLCを必要としない規則は純粋な単体テストで検証し、UIテストはsignal接続と委譲を中心にする。
- 完了前に `scripts/check.ps1 -Check` を実行し、format、ruff、全テスト、カバレッジ基準、`git diff --check` をすべて通す。
- ビルド関連を変更した場合はWindows向けビルドも確認する。
